<div class='beers'>
    {{#each beers}}
        <img src={{this.image_url}} />
        <h2>{{this.name}}</h2>
        <p>{{this.description}}</p>
        <h4>{{this.tagline}}</h4>
    {{/each}}
</div>
